"""Link exception clauses to parent rules (placeholder)."""

from __future__ import annotations

from ..config.schemas import ExtractedRule


def link_exceptions(rules: list[ExtractedRule]) -> list[ExtractedRule]:
    return list(rules)
