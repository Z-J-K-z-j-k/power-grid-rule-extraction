DEEPSEEK_API_KEY=your_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-reasoner

# 抽取并发（默认 2）；若仍出现 incomplete chunked read，可改为 1 串行
# EXTRACT_MAX_WORKERS=2

# API 单次请求失败（断连、429、502 等）时的最大重试次数（默认 6）
# API_MAX_RETRIES=6

# 重试间隔基数秒（指数退避，默认 2）
# API_RETRY_BASE_SECONDS=2
